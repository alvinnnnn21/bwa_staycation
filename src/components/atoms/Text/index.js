import React from 'react';
import { Button } from '..';

const Text = () => {
  return (
      <Button className="brand-text-icon" href="" type="link" isExternal={false}>
          Stay<span className="text-gray-900">cation.</span>
      </Button>
  )
};

export default Text;
